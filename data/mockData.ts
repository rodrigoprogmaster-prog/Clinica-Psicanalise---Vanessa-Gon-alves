import { Patient, Appointment, SessionNote, Transaction, InternalObservation } from '../types';

// All mock data has been cleared to start with a fresh application state.
// You can manually add data through the application's interface.

export const mockPatients: Patient[] = [];

export const mockAppointments: Appointment[] = [];

export const mockNotes: SessionNote[] = [];

export const mockObservations: InternalObservation[] = [];

export const mockTransactions: Transaction[] = [];
